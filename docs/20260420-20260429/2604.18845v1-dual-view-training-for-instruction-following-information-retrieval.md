---
title: Dual-View Training for Instruction-Following Information Retrieval
title_zh: 面向指令跟随信息检索的双视图训练
authors: "Qingcheng Zeng, Puxuan Yu, Aman Mehta, Fuheng Zhao, Rajhans Samdani"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18845v1"
tags: ["query:ai"]
score: 8.0
evidence: 遵循用户约束指令的信息检索
tldr: "指令遵循信息检索要求系统不仅查找相关文档，还需遵守用户约束。针对现有检索器难以区分主题匹配与指令满足的问题，本文提出基于极性反转的双视图数据合成策略：给定查询、相关文档和违反指令的困难负例，利用大语言模型生成互补指令使两者标签互换，迫使检索器通过指令重新评估文档对。该方法在3.05亿参数编码器上将FollowIR基准性能提升45%，超越规模相当或更大的通用模型，证明数据多样性与指令监督的互补性，为构建兼具广泛能力与指令感知的检索系统提供了有效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18845v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 1000, \"height\": 337}]"
motivation: 现有检索器主要针对语义相关性训练，难以区分仅匹配主题与满足指令的文档，导致在遵循指令的检索任务上表现不佳。
method: 提出了一种基于极性反转的双视图数据合成策略，通过生成互补指令使同一文档对在两种指令下交换相关性标签，从而训练检索器关注指令而非固定主题线索。
result: "在3.05亿参数编码器上，该方法将FollowIR基准性能提升45%，超越规模相当或更大的通用嵌入模型，且数据多样性与指令监督具有互补作用。"
conclusion: 研究结果表明，针对性的数据合成对于构建既具备广泛检索能力又遵循指令的检索系统具有重要价值。
---

## 摘要
指令跟随信息检索（IF-IR）研究的是不仅需要找到与查询相关的文档，还必须遵循用户明确约束（如所需属性、排除项或输出偏好）的检索系统。然而，大多数检索模型主要针对语义相关性进行训练，往往难以区分仅符合主题的文档与满足指令的文档。我们提出了一种基于极性反转的双视图数据合成策略：给定一个查询、一个在指令下相关的文档以及一个符合查询但违反指令的困难负例，我们提示大型语言模型生成一个互补指令，使得这两个文档在该指令下交换相关性标签。通过在同一文档对在互补指令下呈现反转的相关性标签，训练信号迫使检索模型通过指令重新考虑相同的候选集，而不是依赖固定的主题线索。在一个3.05亿参数的编码器上，我们的方法在FollowIR基准测试中将性能提升了45%，超越了规模相当或更大的通用嵌入模型。通过在匹配数据预算下的直接比较，我们进一步表明数据多样性和指令监督具有互补作用：前者保持通用检索质量，而后者提高指令敏感性。这些结果突显了针对性数据合成对于构建既具备广泛能力又具有指令感知的检索系统的价值。

## Abstract
Instruction-following information retrieval (IF-IR) studies retrieval systems that must not only find documents relevant to a query, but also obey explicit user constraints such as required attributes, exclusions, or output preferences. However, most retrievers are trained primarily for semantic relevance and often fail to distinguish documents that match the topic from those that satisfy the instruction. We propose a dual-view data synthesis strategy based on polarity reversal: given a query, a document that is relevant under the instruction, and a hard negative that matches the query but violates the instruction, we prompt an LLM to generate a complementary instruction under which the two documents swap relevance labels. By presenting the same document pair under complementary instructions that invert their relevance labels, the training signal forces the retriever to reconsider the same candidate set through the instruction, rather than relying on fixed topical cues. On a 305M-parameter encoder, our method improves performance on the FollowIR benchmark by 45%, surpassing general-purpose embedding models of comparable or larger scale. Through head-to-head comparisons at matched data budgets, we further show that data diversity and instruction supervision play complementary roles: the former preserves general retrieval quality, while the latter improves instruction sensitivity. These results highlight the value of targeted data synthesis for building retrieval systems that are both broadly capable and instruction-aware.

---

## 论文详细总结（自动生成）

### 论文总结：Dual-View Training for Instruction-Following Information Retrieval

#### 1. 核心问题与整体含义
*   **研究背景**：指令跟随信息检索（IF-IR）要求检索系统不仅能找到与查询语义相关的文档，还必须严格遵守用户附加的指令约束（如格式、风格、排除条件等）。这超越了传统的语义匹配。
*   **核心问题**：现有的大多数密集检索模型主要针对通用语义相关性进行训练，导致其难以区分“仅与查询主题相关”的文档和“真正满足具体指令要求”的文档。模型往往过度依赖固定的查询-文档关联，而忽视了动态的指令内容。
*   **研究动机**：为了解决上述问题，提升检索模型对指令的敏感性和遵循能力，本文旨在探索一种高效的数据增强方法，在不增加人工标注成本的前提下，显著提升模型的IF-IR性能。

#### 2. 提出的方法论
*   **核心思想**：**基于极性反转的双视图数据合成**。核心洞察是：一个在原始指令下的“指令负例”（即与查询相关但违反指令的文档），在另一个互补的指令下，应该成为“正例”。
*   **关键技术细节**：
    1.  **数据合成流程**：给定一个原始训练样本（包含查询 `q`、原始指令 `I_orig`、正例文档 `D+`、指令负例文档 `D-`），使用大语言模型（LLM）生成一个新的互补指令 `I_new`。`I_new` 需满足：在 `(q, I_new)` 下，`D-` 变为相关文档（正例），而 `D+` 变为不相关文档（指令负例）。
    2.  **训练目标**：在对比学习框架中，模型同时看到原始视图 `(q ⊕ I_orig, D+)` 和合成视图 `(q ⊕ I_new, D-)` 作为正对。模型必须学会为同一查询在不同指令下检索出完全不同的文档，从而强制其编码器关注指令的细微差别，而非静态的查询-文档相似度。
    3.  **合成提示工程**：论文附录提供了详细的提示模板，指导LLM通过分析文档属性、选择“反转杠杆”（如改变领域、时间范围、受众水平等）来生成高质量、多样化且与原始指令不同的新指令。

#### 3. 实验设计
*   **评估基准**：
    *   **FollowIR**：核心评估集，用于衡量指令敏感性（主要指标：p-MRR）和整体检索质量（Score）。
    *   **InfoSearch**：包含长度和关键词子集，同样报告 p-MRR。
    *   **MAIR**：包含 IFEval 和 InstructIR 子集，报告 nDCG@10。
*   **对比方法（数据配置）**：在固定总数据量的约束下，对比了四种训练数据配置，以厘清数据多样性与指令监督的作用：
    *   **Ins-orig**：仅使用原始指令数据。
    *   **Ins-DV**：一半原始指令数据 + 一半双视图合成数据（总量与Ins-orig相同）。
    *   **All-orig**：原始指令数据 + 其对应的非指令数据（即两倍于Ins-orig的多样数据）。
    *   **All-DV**：原始指令数据 + 双视图合成数据（总量与All-orig相同，但无双视图数据）。
*   **模型骨干**：在 `gte-multilingual-mlm-base` (305M) 和 `bge-m3-retromae` 两个不同的编码器上进行实验，以验证方法的通用性。

#### 4. 资源与算力
*   论文**未明确说明**训练所使用的具体硬件配置（如GPU型号、数量）、训练耗时或总计算开销。
*   文中提及数据合成使用了 **Qwen3-Next-80B-A3B-Instruct** 模型，训练采用了 **Arctic-Embed** 框架。

#### 5. 实验数量与充分性
*   **实验组数**：实验设计系统且充分。主要包含：
    1.  在两个不同的编码器骨干网络上进行主实验。
    2.  在四种不同的数据配置（Ins-orig, Ins-DV, All-orig, All-DV）下进行对比，这本质上是一组核心的消融实验。
    3.  在三个主要的IF-IR评测基准（FollowIR, InfoSearch, MAIR）及其多个子集上进行全面评估。
*   **公平性与客观性**：
    *   **公平**：所有对比均在**严格匹配的数据预算（总样本数）** 下进行，确保了性能差异源于数据构成而非数据量。
    *   **客观**：使用了社区公认的基准和指标（p-MRR, nDCG）。结果清晰显示了不同数据策略的权衡（如Ins-DV提升指令敏感性但损失部分通用分数）。
    *   **充分性**：实验足以支撑其核心结论——双视图数据的有效性，以及数据多样性与指令监督的互补/冲突关系。跨骨干网络的一致性结果增强了结论的可靠性。

#### 6. 主要结论与发现
1.  **双视图训练显著提升指令敏感性**：在305M参数模型上，Ins-DV配置将FollowIR的p-MRR提升了45%，超越了参数量更大的通用嵌入模型（如EmbeddingGemma-300M）。
2.  **揭示数据构成的关键权衡**：
    *   **指令监督**（Ins-DV vs. Ins-orig）：专门针对指令的数据能有效提升IF能力，但若牺牲数据多样性（用合成数据替换部分原始数据），可能会损害通用检索质量（Score下降）。
    *   **数据多样性**（All-orig vs. Ins-orig）：混合非指令数据能提供多样性并保持通用质量，但会严重**稀释**指令信号，导致模型甚至做出与指令相反的判断（InfoSearch上出现负p-MRR）。
3.  **双视图合成实现最佳平衡**：All-DV配置（用双视图数据替代非指令数据）能够**同时提升**指令敏感性（达到最高的p-MRR）和通用检索质量（达到最高的Score），成功调和了上述矛盾。
4.  **方法具有普适性**：上述模式在两个不同的编码器骨干网络上都得到了复现，证明了该方法是模型无关的。

#### 7. 优点
*   **方法新颖巧妙**：利用“极性反转”的视角，将传统的“指令负例”转化为有价值的监督信号来源，思想上有创新。
*   **成本效益高**：完全基于现有数据和大语言模型自动化合成新训练对，无需昂贵的人工标注。
*   **实验设计严谨**：通过精心控制的对比实验（匹配数据量），清晰剥离并论证了“数据多样性”和“指令监督”两个关键因素的不同作用及其冲突，使结论非常扎实。
*   **实用性强**：所提方法不改变现有训练框架（如对比学习），易于集成到现有检索模型训练流程中。

#### 8. 不足与局限
*   **合成假设的局限性**：方法默认对于任何查询-文档对，都存在一个“有意义”的互补指令能反转其相关性。对于指令本身非常狭隘或绝对的情况（例如“仅限某特定日期”），生成的互补指令可能不自然或无效。
*   **模型架构范围有限**：实验仅集中在**编码器**式的双塔检索模型。该方法在解码器式或交叉编码器架构上的有效性尚未得到验证。
*   **语言与领域局限**：所有实验均在**英语**基准上进行。方法在跨语言或多语言检索场景中的表现仍是未知数。
*   **计算资源不透明**：未报告训练所需的计算成本，使得该方法在算力需求方面的可复现性和实用性评估存在困难。
*   **潜在的数据偏差**：合成数据的质量完全依赖于所选用的大语言模型（Qwen3），可能引入该模型固有的偏见或风格，并传递到训练的检索器中。

（完）
